function add(skill) {
  if(skill.add = collection + 1){
    return true;
  }
}
function remove(skill) {
  if(skill.remove = collection -1){
    return true;
  }
}
module.exports = {
  add: add, 
  remove: remove };